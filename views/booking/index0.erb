<p>All bookings </p>

<p>
  Choose one of listed below bookings


   <% @bookings.each do |booking| %>

   <a href="/manager/bookings/stats<%= booking.id %>"> <p>Instructor:<%= booking.instructor_name %>
   Session id: <%=booking.sessions_id%> Member id: <%=booking.members_id%></a> </p>
   <% @sessions.each do |session| %>
   <% if session.members.id == booking.members_id %>
        <option value=<%= session.id %> selected><%= session.name %><%=bookings.member.name%></option>
    <% end %>
    <% end %>

     <% if booking.member.id == booking.sessions_id %>
        <option value=<%= booking.member.id %> selected><%= booking.member.first_name %>
          <%= booking.member.last_name %></option>
      <% else %>
        <option value=<%= booking.member.id %>><%=booking.member.first_name %> <%= booking.member.last_name %></option>
      <% end %>
    <% end %>



    <ul>
      <li> <% for booking in @bookings %>
      <p <a href="/manager/bookings/<%= @id %>">

        <a href="/manager/bookings/<%= booking.id %>"> <p>Instructor:<%= booking.instructor_name %>
        Session id: <%=booking.sessions_id%> Member id: <%=booking.members_id%></a> </p>
     </li>
    <li>
      <p>
        <%= booking.session.name %>  <%= booking.member.first_name %> <%= booking.member.last_name %>



        </p>

      <% end %>
    </li>
    </ul>
