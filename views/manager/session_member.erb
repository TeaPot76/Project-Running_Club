
<p> Members per session:  </p>

<% @members.each do |member| %>
  <p>
    <a href="/manager/sessions/member
      <% member.id %>">
      <% member.first_name %>
      <%  member.last_name%>
      <p> Session_id <%=  member.sessions_id%></p>
      <%= Member.sessions(member.sessions_id)%>
    </a>
  </p>
<%end%>
